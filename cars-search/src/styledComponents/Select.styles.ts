import styled from "styled-components";

export const Select = styled.select`
  background: #fff;
  color: #467996;
  padding: 0.7rem;
  border: none;
  font-size: 1.25rem;
  border-radius: 0.25rem;
  outline: none;
`;
