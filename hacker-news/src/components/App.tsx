import React from "react";
import { SearchForm, Stories, Buttons } from ".";

export const App = () => {
  return (
    <React.Fragment>
      <SearchForm />
      <Buttons />
      <Stories />
    </React.Fragment>
  );
};
